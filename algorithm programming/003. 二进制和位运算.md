![image.png](https://fastly.jsdelivr.net/gh/lefthandYao/my-image-bed@main/pc/20260720205538898.png)

二进制和位的概念
	位（bit）0或1
	字节（byte）8bit 基本存储单元（一个字节对应一个地址）
	字（word）CPU一次读取的数据位数

正数怎么用二进制表达
	原码

负数怎么用二进制表达（只有signed才有负数）
	补码（按位取反+1）
	记住在计算机里1111就代表-1
	最高位是1，就一定是负数
	计算机可以表示从-2^(n-1) ~ 2^(n-1) -1

打印二进制；直接定义二进制、十六进制的变量
	

常见的位运算
	| 
	&
	^
	~
	<<
	>
	>>>
解释打印二进制的函数

| 和 &是位运算不是逻辑运算

相反数

整数最小值的特殊性（取绝对值还是自己）

为什么这么设计二进制

为什么加法逻辑如此重要

关于溢出

位运算的玩法（后面可会详细讲述）
